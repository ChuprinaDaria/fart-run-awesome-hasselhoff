[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "sentinel"
version = "0.1.0"
description = "fart.run sentinel — cross-platform host IDS"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
